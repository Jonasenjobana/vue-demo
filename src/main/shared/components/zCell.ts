import { defineComponent, h } from "vue";

export default defineComponent({
    name: 'ZCell',
    props: {
        field: String,
    },
    provide: {

    },
    inject: {
        
    },
    setup(props, ctx) {
        return h('div')
    }
})